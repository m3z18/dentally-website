import Link from "next/link";import { Container } from "@/components/ui/container";import { getLocale } from "@/lib/i18n";export default async function NotFound(){const en=(await getLocale())==="en";return <Container className="grid min-h-[60vh] place-items-center py-16 text-center"><div><p className="text-sm font-bold text-brand">404</p><h1 className="mt-4 text-4xl font-bold">{en?"Page not found":"الصفحة غير موجودة"}</h1><p className="mt-4 text-muted">{en?"The link may have changed or the content is not published.":"قد يكون الرابط قد تغير أو أن المحتوى غير منشور."}</p><div className="mt-8 flex flex-wrap justify-center gap-3"><Link className="rounded-full bg-brand px-6 py-3 text-sm font-bold text-white" href="/">{en?"Back to home":"العودة للرئيسية"}</Link><Link className="rounded-full border border-line px-6 py-3 text-sm font-bold" href="/search">{en?"Search the website":"البحث في الموقع"}</Link></div></div></Container>}
