import type { Metadata } from "next";import { LegalPage } from "@/components/legal-page";import { getLocale } from "@/lib/i18n";export async function generateMetadata():Promise<Metadata>{return{title:(await getLocale())==="en"?"Appointment policy":"سياسة المواعيد"}}export default function Page(){return <LegalPage titleAr="سياسة المواعيد" titleEn="Appointment policy" summaryAr="لا يتوفر نص معتمد لسياسة المواعيد حاليًا." summaryEn="No approved appointment policy is currently available."/>}
