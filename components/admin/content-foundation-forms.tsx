"use client";

import { useActionState } from "react";
import { saveCategoryAction, toggleCategoryDeletionAction } from "@/app/admin/content/actions";
import { saveFaqAction, toggleFaqDeletionAction } from "@/app/admin/faq/actions";
import { saveSiteSettingsAction } from "@/app/admin/settings/actions";
import type { AdminActionState } from "@/types/admin";
import type { ArticleCategoryRow, FaqRow, SiteSettingsRow } from "@/types/content";

const initial: AdminActionState = { status: "idle", message: "" };
const input = "min-h-11 rounded-xl border border-line bg-background px-3 text-sm font-normal outline-none focus:border-brand";
const area = `${input} min-h-24 resize-y py-3 leading-7`;
const panel = "rounded-card border border-line bg-surface p-5";

export function CategoryForm({ category }: { category?: ArticleCategoryRow }) { const [state, action, pending] = useActionState(saveCategoryAction, initial); return <form action={action} className={`${panel} grid gap-4`}>{category && <input type="hidden" name="id" value={category.id} />}<div className="grid gap-4 md:grid-cols-2"><Field label="الاسم العربي"><input className={input} name="nameAr" required minLength={2} maxLength={120} defaultValue={category?.name_ar ?? ""} /></Field><Field label="English name"><input className={input} name="nameEn" dir="ltr" defaultValue={category?.name_en ?? ""} /></Field><Field label="الرابط المختصر"><input className={input} name="slug" dir="ltr" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" defaultValue={category?.slug ?? ""} /></Field><Field label="الترتيب"><input className={input} name="displayOrder" type="number" min={0} defaultValue={category?.display_order ?? 0} /></Field></div><Field label="الوصف العربي"><textarea className={area} name="descriptionAr" maxLength={500} defaultValue={category?.description_ar ?? ""} /></Field><Field label="English description"><textarea className={area} name="descriptionEn" dir="ltr" maxLength={500} defaultValue={category?.description_en ?? ""} /></Field>{category && !category.deleted_at && <Check name="isActive" label="ظاهر للعامة" checked={category.is_active} />}<Message state={state} /><button disabled={pending} className="min-h-11 justify-self-end rounded-full bg-brand px-5 text-xs font-bold text-white">{category ? "حفظ" : "إضافة مخفية"}</button></form>; }

export function CategoryDeleteForm({ category }: { category: ArticleCategoryRow }) { const [state, action, pending] = useActionState(toggleCategoryDeletionAction, initial); return <form action={action} className="mt-3 flex flex-wrap items-center gap-3"><input type="hidden" name="id" value={category.id} />{category.deleted_at && <input type="hidden" name="restore" value="yes" />}<button disabled={pending} className={`rounded-full px-4 py-2 text-xs font-bold ${category.deleted_at ? "bg-brand text-white" : "bg-red-50 text-red-800"}`}>{category.deleted_at ? "استعادة مخفية" : "حذف منطقي"}</button><Message state={state} /></form>; }

export function FaqForm({ item, services }: { item?: FaqRow; services: Array<{ id: string; name_ar: string }> }) { const [state, action, pending] = useActionState(saveFaqAction, initial); return <form action={action} className={`${panel} grid gap-4`}>{item && <input type="hidden" name="id" value={item.id} />}<Field label="السؤال بالعربية"><input className={input} name="questionAr" required maxLength={300} defaultValue={item?.question_ar ?? ""} /></Field><Field label="الإجابة بالعربية"><textarea className={area} name="answerAr" required maxLength={3000} defaultValue={item?.answer_ar ?? ""} /></Field><div dir="ltr" className="grid gap-4"><Field label="English question"><input className={input} name="questionEn" maxLength={300} defaultValue={item?.question_en ?? ""} /></Field><Field label="English answer"><textarea className={area} name="answerEn" maxLength={3000} defaultValue={item?.answer_en ?? ""} /></Field></div><div className="grid gap-4 md:grid-cols-3"><Field label="تصنيف اختياري"><input className={input} name="category" maxLength={100} defaultValue={item?.category ?? ""} /></Field><Field label="خدمة اختيارية"><select className={input} name="serviceId" defaultValue={item?.service_id ?? ""}><option value="">عام</option>{services.map((service) => <option key={service.id} value={service.id}>{service.name_ar}</option>)}</select></Field><Field label="الترتيب"><input className={input} name="displayOrder" type="number" min={0} defaultValue={item?.display_order ?? 0} /></Field></div>{item && !item.deleted_at && <Check name="isActive" label="ظاهر للعامة" checked={item.is_active} />}<Message state={state} /><button disabled={pending} className="min-h-11 justify-self-end rounded-full bg-brand px-5 text-xs font-bold text-white">{item ? "حفظ" : "إضافة مخفية"}</button></form>; }

export function FaqDeleteForm({ item }: { item: FaqRow }) { const [state, action, pending] = useActionState(toggleFaqDeletionAction, initial); return <form action={action} className="mt-3 flex gap-3"><input type="hidden" name="id" value={item.id} />{item.deleted_at && <input type="hidden" name="restore" value="yes" />}<button disabled={pending} className={`rounded-full px-4 py-2 text-xs font-bold ${item.deleted_at ? "bg-brand text-white" : "bg-red-50 text-red-800"}`}>{item.deleted_at ? "استعادة مخفية" : "حذف منطقي"}</button><Message state={state} /></form>; }

export function SiteSettingsForm({ settings }: { settings: SiteSettingsRow | null }) { const [state, action, pending] = useActionState(saveSiteSettingsAction, initial); const s = settings; return <form action={action} className="mt-8 grid gap-6"><Section title="بيانات المنشأة"><Grid><Field label="الاسم بالعربية"><input className={input} name="organizationNameAr" defaultValue={s?.organization_name_ar ?? ""} /></Field><Field label="English name"><input className={input} name="organizationNameEn" dir="ltr" defaultValue={s?.organization_name_en ?? ""} /></Field><Field label="نبذة عربية"><textarea className={area} name="aboutAr" defaultValue={s?.about_ar ?? ""} /></Field><Field label="About"><textarea className={area} name="aboutEn" dir="ltr" defaultValue={s?.about_en ?? ""} /></Field><Field label="الرؤية"><textarea className={area} name="visionAr" defaultValue={s?.vision_ar ?? ""} /></Field><Field label="Vision"><textarea className={area} name="visionEn" dir="ltr" defaultValue={s?.vision_en ?? ""} /></Field><Field label="الرسالة"><textarea className={area} name="missionAr" defaultValue={s?.mission_ar ?? ""} /></Field><Field label="Mission"><textarea className={area} name="missionEn" dir="ltr" defaultValue={s?.mission_en ?? ""} /></Field><Field label="القيم العربية، قيمة في كل سطر"><textarea className={area} name="valuesAr" defaultValue={s?.values_ar?.join("\n") ?? ""} /></Field><Field label="English values"><textarea className={area} name="valuesEn" dir="ltr" defaultValue={s?.values_en?.join("\n") ?? ""} /></Field></Grid></Section><Section title="التواصل"><Grid><Field label="الهاتف"><input className={input} name="phone" dir="ltr" defaultValue={s?.phone ?? ""} /></Field><Field label="واتساب"><input className={input} name="whatsapp" dir="ltr" defaultValue={s?.whatsapp ?? ""} /></Field><Field label="البريد"><input className={input} name="email" type="email" dir="ltr" defaultValue={s?.email ?? ""} /></Field><Field label="رابط الخريطة"><input className={input} name="mapsUrl" type="url" dir="ltr" defaultValue={s?.maps_url ?? ""} /></Field><Field label="العنوان"><textarea className={area} name="addressAr" defaultValue={s?.address_ar ?? ""} /></Field><Field label="Address"><textarea className={area} name="addressEn" dir="ltr" defaultValue={s?.address_en ?? ""} /></Field><Field label="ساعات العمل"><textarea className={area} name="workingHoursAr" defaultValue={s?.working_hours_ar ?? ""} /></Field><Field label="Working hours"><textarea className={area} name="workingHoursEn" dir="ltr" defaultValue={s?.working_hours_en ?? ""} /></Field><Field label="Latitude"><input className={input} name="latitude" type="number" step="0.000001" dir="ltr" defaultValue={s?.latitude ?? ""} /></Field><Field label="Longitude"><input className={input} name="longitude" type="number" step="0.000001" dir="ltr" defaultValue={s?.longitude ?? ""} /></Field></Grid></Section><Section title="الروابط الاجتماعية"><Grid>{["instagram","x","facebook","youtube","snapchat"].map((key) => <Field key={key} label={key}><input className={input} name={key} type="url" dir="ltr" defaultValue={socialValue(s?.social_links, key)} /></Field>)}</Grid></Section><Section title="SEO والتنبيه الطبي"><Grid><Field label="عنوان SEO الافتراضي"><input className={input} name="defaultSeoTitleAr" maxLength={70} defaultValue={s?.default_seo_title_ar ?? ""} /></Field><Field label="Default SEO title"><input className={input} name="defaultSeoTitleEn" maxLength={70} dir="ltr" defaultValue={s?.default_seo_title_en ?? ""} /></Field><Field label="وصف SEO"><textarea className={area} name="defaultSeoDescriptionAr" maxLength={180} defaultValue={s?.default_seo_description_ar ?? ""} /></Field><Field label="SEO description"><textarea className={area} name="defaultSeoDescriptionEn" maxLength={180} dir="ltr" defaultValue={s?.default_seo_description_en ?? ""} /></Field><Field label="التنبيه الطبي"><textarea className={area} name="medicalDisclaimerAr" defaultValue={s?.medical_disclaimer_ar ?? ""} /></Field><Field label="Medical disclaimer"><textarea className={area} name="medicalDisclaimerEn" dir="ltr" defaultValue={s?.medical_disclaimer_en ?? ""} /></Field></Grid></Section><Section title="إعلان اختياري"><Grid><Field label="النص العربي"><input className={input} name="announcementAr" defaultValue={s?.announcement_ar ?? ""} /></Field><Field label="English text"><input className={input} name="announcementEn" dir="ltr" defaultValue={s?.announcement_en ?? ""} /></Field><Field label="الرابط"><input className={input} name="announcementUrl" dir="ltr" defaultValue={s?.announcement_url ?? ""} /></Field><Check name="announcementIsActive" label="تفعيل الإعلان" checked={s?.announcement_is_active ?? false} /></Grid></Section><Message state={state} /><button disabled={pending} className="min-h-12 justify-self-end rounded-full bg-brand px-7 text-sm font-bold text-white">حفظ الإعدادات</button></form>; }

function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="grid gap-1.5 text-xs font-bold text-muted"><span>{label}</span>{children}</label>; }
function Grid({ children }: { children: React.ReactNode }) { return <div className="grid gap-4 md:grid-cols-2">{children}</div>; }
function Section({ title, children }: { title: string; children: React.ReactNode }) { return <section className={`${panel} grid gap-5`}><h2 className="text-lg font-bold">{title}</h2>{children}</section>; }
function Check({ name, label, checked }: { name: string; label: string; checked: boolean }) { return <label className="flex items-center gap-3 self-center text-sm font-semibold"><input type="checkbox" name={name} defaultChecked={checked} className="size-4 accent-brand" />{label}</label>; }
function Message({ state }: { state: AdminActionState }) { return state.message ? <p role={state.status === "error" ? "alert" : "status"} className={`text-xs ${state.status === "error" ? "text-red-700" : "text-emerald-700"}`}>{state.message}</p> : null; }
function socialValue(value: SiteSettingsRow["social_links"] | undefined, key: string) { return value && typeof value === "object" && !Array.isArray(value) && typeof value[key] === "string" ? value[key] : ""; }
